# Темы

Тематические подборки слов и фраз. Страницы генерируются автоматически из метаданных в MD-файлах (`topics` во frontmatter или группы `##` в readme категорий).

Список тем обновляется при сборке сайта.
